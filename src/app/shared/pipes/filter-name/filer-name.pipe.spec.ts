import { FilerNamePipe } from './filer-name.pipe';

describe('FilerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
